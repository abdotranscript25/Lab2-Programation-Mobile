<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:gravity="center"
    tools:ignore="ExtraText">

    <!-- Titre -->
    <TextView
        android:id="@+id/textView2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:text="Calcul des impôts Locaux"
        android:textColor="#1976D2"
        android:textSize="23sp"
        android:textStyle="bold"
        android:gravity="center" />

    <!-- Nom du propriétaire -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Nom du propriétaire :"
        android:textStyle="bold"
        android:layout_marginBottom="4dp" />

    <EditText
        android:id="@+id/input_nom"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Ex: Abdelhamid El Hachimi"
        android:inputType="textPersonName"
        android:padding="12dp"
        android:background="#FFFFFF"
        android:layout_marginBottom="16dp" />


    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Adresse :"
        android:textStyle="bold"
        android:layout_marginBottom="4dp" />

    <EditText
        android:id="@+id/inputAdresse"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Ex: 6 rue de la gare "
        android:inputType="textPersonName"
        android:padding="12dp"
        android:background="#FFFFFF"
        android:layout_marginBottom="16dp" />

    <!-- Surface -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Surface (m²) :"
        android:textStyle="bold"
        android:layout_marginBottom="4dp" />

    <EditText
        android:id="@+id/inputSurface"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Surface (m²)"
        android:inputType="numberDecimal"
        android:padding="12dp"
        android:background="#FFFFFF"
        android:layout_marginBottom="16dp" />

    <!-- Nombre de pièces -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Nombre de pièces :"
        android:textStyle="bold"
        android:layout_marginBottom="4dp" />

    <EditText
        android:id="@+id/inputdePieces"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Ex: 5"
        android:inputType="number"
        android:padding="12dp"
        android:background="#FFFFFF"
        android:layout_marginBottom="16dp" />

    <!-- CheckBox Piscine -->
    <CheckBox
        android:id="@+id/checkboxdePiscine"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Piscine"
        android:layout_marginBottom="16dp" />

    <!-- Bouton Calculer -->
    <Button
        android:id="@+id/buttondeCalcul"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Calculer"
        android:layout_marginBottom="20dp" />

    <!-- Résultat -->
    <TextView
        android:id="@+id/result"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:layout_marginTop="20dp" />

    <!-- Pied de page avec votre nom (optionnel) -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Créé par Abdelhamid El Hachimi"
        android:textSize="12sp"
        android:textColor="#9E9E9E"
        android:layout_marginTop="24dp" />

</LinearLayout>